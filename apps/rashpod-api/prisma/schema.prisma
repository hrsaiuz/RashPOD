generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  SUPER_ADMIN
  ADMIN
  OPERATIONS_MANAGER
  MODERATOR
  PRODUCTION_STAFF
  FINANCE_STAFF
  SUPPORT_STAFF
  DESIGNER
  CUSTOMER
  CORPORATE_CLIENT
}

enum DesignerStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum DesignStatus {
  DRAFT
  PENDING_MODERATION
  SUBMITTED
  NEEDS_FIX
  APPROVED_LOCAL
  APPROVED_GLOBAL
  APPROVED
  REJECTED
  SUSPENDED
  READY_FOR_MOCKUP
  READY_TO_PUBLISH
  PUBLISHED
}

enum ModerationDecision {
  APPROVE
  REJECT
  REQUEST_CHANGES
  SUSPEND
}

enum GeneratedAssetStatus {
  PENDING
  UPLOADED
  VERIFYING
  PROCESSING
  READY
  FAILED
  EXPIRED
  REPLACED
  ARCHIVED
}

enum AssetPurpose {
  DESIGN_ORIGINAL
  DESIGN_NORMALIZED
  FILM_SOURCE
  MOCKUP_IMAGE
  LISTING_IMAGE
  PRODUCTION_FILE
  MARKETPLACE_EXPORT
  POD_PROVIDER_FILE
  TEMPLATE_IMAGE
  PRINT_AREA_PREVIEW
  EXTERNAL_ORDER_IMPORT
}

enum AssetStorageProvider {
  GCS
  LOCAL_DEV
}

enum AssetAccessPolicy {
  PRIVATE_SIGNED_URL
  PUBLIC_READ
  INTERNAL_ONLY
}

enum AssetLifecycleStatus {
  PENDING_UPLOAD
  UPLOADED
  VERIFYING
  READY
  PROCESSING
  FAILED
  EXPIRED
  REPLACED
  ARCHIVED
}

enum WorkerJobStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}

enum ListingType {
  PRODUCT
  FILM
}

enum FilmOrderKind {
  DESIGN_FILM
  CUSTOM_FILM
}

enum FilmType {
  DTF
  UV_DTF
}

enum FilmConsentAction {
  ENABLED
  REVOKED
  ADMIN_ENABLED
  ADMIN_REVOKED
}

enum ListingStatus {
  DRAFT
  READY_FOR_REVIEW
  READY_TO_PUBLISH
  PUBLISHED
  UNPUBLISHED
  FAILED
  ARCHIVED
  REJECTED
  SUSPENDED
}

enum PipelineType {
  LOCAL
  GLOBAL_PRINTFUL
  GLOBAL_POD
}

enum ProviderType {
  RASHPOD
  PRINTFUL
  PRINTIFY
  DIRECT_MARKETPLACE
}

enum ModerationPipelineDecision {
  REJECT
  APPROVE_LOCAL
  APPROVE_GLOBAL
}

enum RejectionReasonCode {
  COPYRIGHT_RISK
  OFFENSIVE_CONTENT
  POLITICAL_SENSITIVE_CONTENT
  LOW_IMAGE_RESOLUTION
  POOR_IMAGE_QUALITY
  WRONG_FILE_FORMAT
  TRANSPARENCY_OR_BACKGROUND_ISSUE
  NOT_SUITABLE_FOR_PRODUCTION
  DUPLICATE_OR_SPAM
  MARKETPLACE_COMPLIANCE_RISK
  OTHER
}

enum LocalProductType {
  T_SHIRT
  LONG_SLEEVE_T_SHIRT
  HOODIE
  SWEATSHIRT
  MUG
  CAP
  TOTE_BAG
  POSTER
  STICKER
  DTF_FILM
  UV_DTF_FILM
  OTHER
}

enum NecklineType {
  CREW_NECK
  V_NECK
  POLO
  HOODIE
  NONE
}

enum SleeveType {
  SHORT
  LONG
  SLEEVELESS
  NONE
}

enum LocalProductionMethod {
  DTF
  UV_DTF
  DTG
  SUBLIMATION
  EMBROIDERY
  OTHER
}

enum PlacementKind {
  FRONT
  BACK
  LEFT_CHEST
  RIGHT_CHEST
  LEFT_SLEEVE
  RIGHT_SLEEVE
  FULL_WRAP
  OTHER
}

enum PlacementAlignment {
  CENTER
  TOP_CENTER
  LEFT_CHEST
  CUSTOM
}

enum PlacementUnits {
  CM
  INCH
  PX
}

enum DesignProductSelectionStatus {
  SELECTED
  MOCKUP_PENDING
  MOCKUP_GENERATING
  MOCKUP_READY
  MOCKUP_FAILED
  LISTING_DRAFT
  READY_TO_PUBLISH
  PUBLISHED
  FAILED
}

enum MockupAssetType {
  MAIN
  SECONDARY
  DETAIL
  LIFESTYLE
  PRINT_AREA_PREVIEW
}

enum MockupAssetStatus {
  PENDING
  PROCESSING
  GENERATED
  READY
  FAILED
  REPLACED
  ARCHIVED
}

enum MarketplaceKind {
  RASHPOD_LOCAL
  LOCAL_MARKETPLACE
  ETSY
  AMAZON
  EBAY
  SHOPIFY
  WOOCOMMERCE
}

enum MarketplacePublicationStatus {
  NOT_SELECTED
  DRAFT
  QUEUED
  PUBLISHING
  PUBLISHED
  FAILED
  NEEDS_REVIEW
}

enum MarketplaceType {
  MANUAL_EXPORT
}

enum ManualMarketplaceKey {
  UZUM
  OLX_UZ
  TELEGRAM_SHOP
  INSTAGRAM
  FACEBOOK
  OTHER
}

enum MarketplaceExportFormat {
  CSV
  XLSX
  ZIP_IMAGES
  CSV_WITH_IMAGE_URLS
}

enum MarketplaceExportBatchStatus {
  DRAFT
  READY
  EXPORTING
  EXPORTED
  FAILED
  CANCELED
  PARTIALLY_EXPORTED
}

enum MarketplaceExportItemStatus {
  DRAFT
  READY
  BLOCKED
  EXPORTED
  FAILED
  CANCELED
}

enum ExportedListingStatus {
  EXPORTED
  MANUALLY_LISTED
  ACTIVE
  PAUSED
  SOLD_EXTERNALLY
  ARCHIVED
  ERROR
}

enum MarketplaceExternalSaleStatus {
  RECORDED
  ORDER_CREATED
  CANCELED
}

enum MarketplaceExternalSaleSource {
  MANUAL
}

enum ExternalOrderSourceType {
  MARKETPLACE_MANUAL
  MARKETPLACE_CSV_IMPORT
  TELEGRAM_MANUAL
  INSTAGRAM_MANUAL
  PHONE_ORDER
  SHOWROOM_OR_OFFLINE
  GLOBAL_POD_PROVIDER_MANUAL
  OTHER
}

enum ExternalOrderPaymentStatus {
  PAID_EXTERNALLY
  CASH_ON_DELIVERY
  PAYMENT_PENDING_MANUAL
  UNPAID
  REFUNDED_EXTERNALLY
  CANCELED_EXTERNALLY
  MANUAL_REVIEW
}

enum ExternalOrderIntakeStatus {
  DRAFT
  VALIDATING
  READY_TO_CONVERT
  CONVERTED_TO_ORDER
  NEEDS_REVIEW
  DUPLICATE
  CANCELED
  FAILED
}

enum ExternalOrderItemMappingStatus {
  MAPPED
  AUTO_MAPPED
  NEEDS_MAPPING
  NOT_FOUND
  AMBIGUOUS
}

enum ExternalOrderDuplicateStatus {
  UNIQUE
  POSSIBLE_DUPLICATE
  CONFIRMED_DUPLICATE
  DUPLICATE_IGNORED
}

enum ExternalOrderImportStatus {
  UPLOADED
  PARSED
  MAPPED
  VALIDATED
  IMPORTED
  FAILED
}

enum PodProviderType {
  PRINTFUL
  PRINTIFY
}

enum PodProviderMode {
  TEST
  LIVE
}

enum PodCatalogSyncStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
}

enum PodAvailabilityStatus {
  AVAILABLE
  UNAVAILABLE
  ARCHIVED
}

enum PodMappingQuality {
  EXACT
  COMPATIBLE
  MANUAL_REVIEW
}

enum PodProviderFileStatus {
  PENDING
  UPLOADED
  FAILED
}

enum PodSyncRecordStatus {
  DRAFT
  VALIDATING
  READY
  SYNCING
  SYNCED
  FAILED
  MANUAL_REVIEW
  CANCELED
}

enum PodMockupStatus {
  PENDING
  READY
  FAILED
}

enum PodWebhookEventStatus {
  ACCEPTED
  REJECTED
  PROCESSED
  FAILED
}

enum OrderFulfillmentRoute {
  LOCAL_PRODUCTION
  GLOBAL_POD_PROVIDER
  MANUAL_EXTERNAL
}

enum PrintfulMappingStatus {
  PENDING
  READY
  FAILED
}

enum IntegrationLogStatus {
  PENDING
  SUCCESS
  FAILED
  SKIPPED
}

enum OrderStatus {
  PENDING_PAYMENT
  PAID
  IN_PRODUCTION
  READY_FOR_PICKUP
  SHIPPED
  DELIVERED
  CANCELLED
  REFUNDED
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  CANCELLED
  REFUNDED
  PARTIALLY_REFUNDED
}

enum ProductionJobStatus {
  ORDERED
  WAITING_FOR_FILE
  FILE_GENERATING
  FILE_CHECK
  READY_FOR_PRINT
  IN_PRODUCTION
  PRINTING
  QUALITY_CHECK
  QC
  QC_FAILED
  REPRINT_REQUIRED
  PACKING
  READY_FOR_PICKUP
  READY_FOR_DELIVERY
  OUT_FOR_DELIVERY
  DELIVERED
  COMPLETED
  CANCELED
  BLOCKED
}

enum CorporateRequestStatus {
  OPEN
  IN_REVIEW
  BIDDING
  OFFER_PENDING
  ACCEPTED
  REJECTED
}

enum DesignerBidStatus {
  SUBMITTED
  SHORTLISTED
  SELECTED
  REJECTED
}

enum CommercialOfferStatus {
  DRAFT
  SENT
  ACCEPTED
  REJECTED
}

enum ExchangeRateSource {
  MANUAL
  SYSTEM
}

enum LedgerEntryType {
  SALE
  ROYALTY
  BONUS
  PAYOUT
  ADJUSTMENT
  REFUND
}

enum LedgerEntryStatus {
  PENDING
  POSTED
  VOID
}

enum RoyaltyEntryType {
  PENDING
  EARNED
  PAYABLE
  PAID
  REVERSED
  ADJUSTMENT
}

enum RoyaltyLedgerStatus {
  CALCULATED
  PENDING
  EARNED
  PAYABLE
  PAID
  ACKNOWLEDGED
  PAIDOUT
  REVERSED
  ADJUSTMENT
  CANCELLED
}

enum PayoutStatus {
  DRAFT
  REQUESTED
  APPROVED
  PROCESSING
  PAID
  CONFIRMED
  FAILED
  CANCELLED
  CANCELED
}

enum PayoutMethod {
  CLICK_WALLET
  BANK_ACCOUNT
  MANUAL
}

enum PaymentReconciliationStatus {
  UNRECONCILED
  MATCHED
  MISMATCHED
  MANUAL_REVIEW
}

enum BonusType {
  MANUAL
  GROUP
  PERFORMANCE
  CAMPAIGN
}

enum BonusStatus {
  DRAFT
  APPLIED
  CANCELLED
}

model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String
  displayName  String
  bio          String?
  avatarUrl    String?
  coverUrl     String?
  handle       String?  @unique
  socialLinks  Json?
  role         UserRole
  designerStatus DesignerStatus @default(ACTIVE)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  designAssets DesignAsset[]
  auditLogs    AuditLog[]
  moderationCases DesignModerationCase[] @relation("ReviewerCases")
  listings     CommerceListing[] @relation("ListingDesigner")
  orders       Order[] @relation("CustomerOrders")
  corporateRequests CorporateRequest[] @relation("CorporateClientRequests")
  designerBids DesignerBid[] @relation("BidDesigner")
  preferences  UserPreferences?
  ledgerEntries LedgerEntry[] @relation("DesignerLedgerEntries")
  royaltyLedgerEntries RoyaltyLedgerEntry[] @relation("DesignerRoyaltyLedgerEntries")
  payouts      Payout[] @relation("DesignerPayouts")
  bonuses      DesignerBonus[] @relation("DesignerBonuses")
  createdBonuses DesignerBonus[] @relation("BonusCreator")
  createdGroupBonuses GroupBonus[] @relation("GroupBonusCreator")
  supportRequests SupportRequest[] @relation("SupportRequestRequester")
  filmConsentEvents FilmConsentEvent[] @relation("FilmConsentEventActor")
  externalOrderIntakes ExternalOrderIntake[] @relation("ExternalOrderIntakeCreator")
  externalOrderImports ExternalOrderImport[] @relation("ExternalOrderImportCreator")
}

model UserPreferences {
  id                 String   @id @default(uuid())
  userId             String   @unique
  portfolioJson      Json?
  payoutDetailsJson  Json?
  notificationJson   Json?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model DesignAsset {
  id          String       @id @default(uuid())
  designerId  String
  title       String
  description String?
  tags        Json?
  categoryId  String?
  originalFileUrl String?
  processedFileUrl String?
  transparentFileUrl String?
  previewUrl  String?
  widthPx     Int?
  heightPx    Int?
  dpi         Int?
  fileType    String?
  fileSize    Int?
  status      DesignStatus @default(DRAFT)
  moderationDecision String?
  moderationStatus String?
  moderatedById String?
  moderatedAt DateTime?
  rejectionReasons Json?
  customRejectionReason String?
  copyrightRiskStatus String?
  contentSafetyStatus String?
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  designer         User              @relation(fields: [designerId], references: [id])
  versions         DesignVersion[]
  commercialRights CommercialRights?
  moderationCases  DesignModerationCase[]
  moderationAudits ModerationAudit[]
  placements       MockupPlacement[]
  productSelections DesignProductSelection[]
  mockupAssets     MockupAsset[]
  printfulFileMappings PrintfulFileMapping[]
  listings         CommerceListing[]
  supportRequests  SupportRequest[]
  filmConsentEvents FilmConsentEvent[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]
}

model DesignVersion {
  id            String   @id @default(uuid())
  designAssetId String
  fileKey       String
  widthPx       Int?
  heightPx      Int?
  dpi           Int?
  hasTransparency Boolean?
  createdAt     DateTime @default(now())

  designAsset DesignAsset @relation(fields: [designAssetId], references: [id])
  moderationCases DesignModerationCase[]
  placements      MockupPlacement[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]
}

model DesignModerationCase {
  id              String             @id @default(uuid())
  designAssetId   String
  designVersionId String?
  reviewerId      String?
  decision        ModerationDecision
  reason          String?
  createdAt       DateTime           @default(now())

  designAsset   DesignAsset   @relation(fields: [designAssetId], references: [id])
  designVersion DesignVersion? @relation(fields: [designVersionId], references: [id])
  reviewer      User?         @relation("ReviewerCases", fields: [reviewerId], references: [id])
}

model ModerationAudit {
  id              String                     @id @default(uuid())
  designId        String
  moderatorId     String
  decision        ModerationPipelineDecision
  predefinedReasons Json?
  customReason    String?
  notes           String?
  beforeStatus    DesignStatus
  afterStatus     DesignStatus
  createdAt       DateTime                   @default(now())

  design DesignAsset @relation(fields: [designId], references: [id], onDelete: Cascade)

  @@index([designId, createdAt])
  @@index([moderatorId])
}

model FileAsset {
  id                 String               @id @default(uuid())
  ownerId            String
  purpose            AssetPurpose         @default(DESIGN_ORIGINAL)
  storageProvider    AssetStorageProvider @default(GCS)
  accessPolicy       AssetAccessPolicy    @default(PRIVATE_SIGNED_URL)
  bucket             String
  objectKey          String               @unique
  publicUrl          String?
  mimeType           String
  fileExtension      String?
  sizeBytes          Int
  widthPx            Int?
  heightPx           Int?
  dpi                Int?
  isPublic           Boolean              @default(false)
  uploadStatus       String
  status             AssetLifecycleStatus @default(PENDING_UPLOAD)
  checksum           String?
  hashSha256         String?
  sourceAssetId      String?
  designId           String?
  designVersionId    String?
  listingId          String?
  mockupTemplateId   String?
  baseProductId      String?
  printAreaId        String?
  placementSnapshot  Json?
  failureReason      String?
  uploadExpiresAt    DateTime?
  uploadedAt         DateTime?
  verifiedAt         DateTime?
  replacedByAssetId  String?
  archivedAt         DateTime?
  createdAt          DateTime             @default(now())
  updatedAt          DateTime             @updatedAt

  marketplaceExportBatches    MarketplaceExportBatch[] @relation("MarketplaceExportFile")
  marketplaceExportZipBatches MarketplaceExportBatch[] @relation("MarketplaceExportZipFile")
  externalOrderImports        ExternalOrderImport[]
  externalOrderIntakeItems    ExternalOrderIntakeItem[]
  podProviderFiles            PodProviderFile[]

  @@index([ownerId, purpose, status])
  @@index([designId])
  @@index([listingId])
}

model CommercialRights {
  id                         String    @id @default(uuid())
  designAssetId              String    @unique
  allowProductSales          Boolean   @default(false)
  allowMarketplacePublishing Boolean   @default(false)
  allowFilmSales             Boolean   @default(false)
  allowCorporateBidding      Boolean   @default(false)
  filmConsentGrantedAt       DateTime?
  filmConsentRevokedAt       DateTime?
  filmConsentVersionId       String?
  filmRoyaltyRate            Float?
  updatedAt                  DateTime  @updatedAt

  designAsset DesignAsset @relation(fields: [designAssetId], references: [id])
}

model FilmConsentEvent {
  id                 String            @id @default(uuid())
  designAssetId      String
  designerId         String
  actorId            String?
  designVersionId    String?
  action             FilmConsentAction
  reason             String?
  policySnapshotJson Json?
  royaltySnapshotJson Json?
  createdAt          DateTime          @default(now())

  designAsset DesignAsset @relation(fields: [designAssetId], references: [id], onDelete: Cascade)
  actor       User?       @relation("FilmConsentEventActor", fields: [actorId], references: [id], onDelete: SetNull)

  @@index([designAssetId, createdAt])
  @@index([designerId, createdAt])
  @@index([actorId])
}

model ProductType {
  id                     String   @id @default(uuid())
  name                   String
  slug                   String   @unique
  category               String
  isActive               Boolean  @default(true)
  availableForDesigners  Boolean  @default(true)
  availableInShop        Boolean  @default(true)
  availableForCorporate  Boolean  @default(true)
  availableForMarketplace Boolean @default(false)
  requiresMockup         Boolean  @default(true)
  supportsFilmSale       Boolean  @default(false)
  productionMethod       String
  baseCost               Decimal? @db.Decimal(10, 2)
  defaultMargin          Decimal? @db.Decimal(10, 2)
  metadataJson           Json?
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  baseProducts           BaseProduct[]
  marketplaceCategoryMappings MarketplaceCategoryMapping[]
  podProductMappings     PodProductMapping[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]
}

model RoyaltyRule {
  id          String   @id @default(uuid())
  scope       String
  basis       String
  value       Decimal  @db.Decimal(10, 4)
  isActive    Boolean  @default(true)
  effectiveAt DateTime
  metadataJson Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  royaltyLedgerEntries RoyaltyLedgerEntry[]
}

model BaseProduct {
  id              String   @id @default(uuid())
  productTypeId   String
  name            String
  slug            String?
  localProductType LocalProductType?
  neckline        NecklineType?
  sleeveType      SleeveType?
  skuPrefix       String
  isActive        Boolean  @default(true)
  baseCost        Decimal? @db.Decimal(10, 2)
  defaultPrice    Decimal? @db.Decimal(10, 2)
  currency        String   @default("UZS")
  imageUrl        String?
  description     String?
  baseImageUrls   Json?
  mockupSceneImages Json?
  printableAreas  Json?
  availableColors Json?
  availableSizes  Json?
  localProductionMethod LocalProductionMethod?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  productType     ProductType      @relation(fields: [productTypeId], references: [id], onDelete: Restrict)
  mockupTemplates MockupTemplate[]
  placementPresets PlacementPreset[]
  productSelections DesignProductSelection[]
  listings CommerceListing[]
  marketplaceCategoryMappings MarketplaceCategoryMapping[]
  podProductMappings PodProductMapping[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]
}

model MockupTemplate {
  id               String   @id @default(uuid())
  baseProductId    String
  name             String
  baseImageKey     String
  lifestyleImageKey String?
  closeupImageKey  String?
  isActive         Boolean  @default(true)
  sortOrder        Int      @default(0)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  baseProduct BaseProduct @relation(fields: [baseProductId], references: [id])
  printAreas  PrintArea[]
  placements  MockupPlacement[]
}

model PrintArea {
  id               String   @id @default(uuid())
  mockupTemplateId String
  name             String
  placement        PlacementKind?
  widthCm          Float?
  heightCm         Float?
  x                Int
  y                Int
  width            Int
  height           Int
  safeZonePx       Int?
  defaultPresetId  String?
  isActive         Boolean @default(true)
  safeX            Int
  safeY            Int
  safeWidth        Int
  safeHeight       Int
  allowMove        Boolean  @default(true)
  allowResize      Boolean  @default(true)
  allowRotate      Boolean  @default(false)
  minScale         Float    @default(0.1)
  maxScale         Float    @default(2)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  mockupTemplate MockupTemplate @relation(fields: [mockupTemplateId], references: [id])
  placements     MockupPlacement[]
  podPrintAreaMappings PodPrintAreaMapping[]
}

model PrintfulProductTemplate {
  id                       String       @id @default(uuid())
  rashpodProductType        String
  displayName               String
  provider                  ProviderType @default(PRINTFUL)
  printfulCatalogProductId  String
  printfulProductName       String
  printfulVariantIds        Json
  allowedColorVariantIds    Json?
  allowedSizeVariantIds     Json?
  allowedPlacements         Json
  allowedTechniques         Json
  defaultTechnique          String
  defaultPlacement          String
  printfulStoreId           String?
  defaultRetailPrice        Decimal? @db.Decimal(10, 2)
  estimatedBaseCost         Decimal? @db.Decimal(10, 2)
  currency                  String   @default("USD")
  active                    Boolean  @default(true)
  metadataJson              Json?
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt

  placementPresets PlacementPreset[]
  productSelections DesignProductSelection[]
  listings CommerceListing[]

  @@unique([provider, printfulCatalogProductId, displayName])
}

model PlacementPreset {
  id                      String             @id @default(uuid())
  name                    String
  pipeline                PipelineType
  productTemplateId       String?
  localBaseProductId      String?
  placement               PlacementKind
  defaultWidthCm          Float?
  defaultHeightCm         Float?
  defaultWidthIn          Float?
  defaultHeightIn         Float?
  defaultX                Float?
  defaultY                Float?
  defaultScale            Float              @default(1)
  alignment               PlacementAlignment @default(CENTER)
  units                   PlacementUnits     @default(CM)
  active                  Boolean            @default(true)
  createdAt               DateTime           @default(now())
  updatedAt               DateTime           @updatedAt

  printfulProductTemplate PrintfulProductTemplate? @relation(fields: [productTemplateId], references: [id])
  localBaseProduct        BaseProduct?             @relation(fields: [localBaseProductId], references: [id])
  productSelections       DesignProductSelection[]

  @@index([pipeline, active])
}

model FilmSaleSettings {
  id                        String   @id @default(uuid())
  enableFilmSalesGlobally   Boolean  @default(false)
  enableDTF                 Boolean  @default(true)
  enableUvDtf               Boolean  @default(true)
  defaultRoyaltyBasis       String
  defaultRoyaltyValue       Decimal  @db.Decimal(10, 4)
  minimumOrderPrice         Decimal? @db.Decimal(10, 2)
  rushOrderFee              Decimal? @db.Decimal(10, 2)
  revocationPolicy          String?
  currency                  String   @default("UZS")
  dtfPricingJson            Json?
  uvDtfPricingJson          Json?
  consentPolicyJson         Json?
  royaltyPolicyJson         Json?
  productionConstraintsJson Json?
  settingsVersion           Int      @default(1)
  taxRatePercent            Decimal? @db.Decimal(10, 4)
  updatedAt                 DateTime @updatedAt
}

model DeliverySetting {
  id                    String   @id @default(uuid())
  providerType          String
  displayName           String
  isActive              Boolean  @default(true)
  zone                  String
  price                 Decimal? @db.Decimal(10, 2)
  freeDeliveryThreshold Decimal? @db.Decimal(10, 2)
  etaText               String?
  metadataJson          Json?
  updatedAt             DateTime @updatedAt
}

model MockupPlacement {
  id               String   @id @default(uuid())
  designAssetId    String
  designVersionId  String
  mockupTemplateId String
  printAreaId      String
  x                Int
  y                Int
  scale            Float
  rotation         Float    @default(0)
  width            Int
  height           Int
  approvedByDesigner Boolean @default(false)
  approvedAt       DateTime?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  designAsset    DesignAsset    @relation(fields: [designAssetId], references: [id])
  designVersion  DesignVersion  @relation(fields: [designVersionId], references: [id])
  mockupTemplate MockupTemplate @relation(fields: [mockupTemplateId], references: [id])
  printArea      PrintArea      @relation(fields: [printAreaId], references: [id])
  generatedAssets GeneratedAsset[]
}

model GeneratedAsset {
  id                String               @id @default(uuid())
  sourcePlacementId String
  type              String
  status            GeneratedAssetStatus @default(PENDING)
  fileKey           String?
  objectKey         String?
  contentType       String?
  format            String?
  widthPx           Int?
  heightPx          Int?
  dpi               Int?
  checksum          String?
  sourceAssetId     String?
  placementSnapshot Json?
  renderJobId       String?
  errorMessage      String?
  failureReason     String?
  replacedByAssetId String?
  archivedAt        DateTime?
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt

  sourcePlacement MockupPlacement @relation(fields: [sourcePlacementId], references: [id])
}

model DesignProductSelection {
  id                         String                       @id @default(uuid())
  designId                   String
  pipeline                   PipelineType
  localBaseProductId          String?
  printfulProductTemplateId   String?
  placementPresetId           String
  placement                   PlacementKind
  technique                   String?
  width                       Float?
  height                      Float?
  x                           Float?
  y                           Float?
  top                         Float?
  left                        Float?
  scale                       Float                        @default(1)
  rotation                    Float                        @default(0)
  units                       PlacementUnits               @default(CM)
  positionHash                String
  placementConfigJson          Json?
  targetMarketplaces          Json?
  selectedByModeratorId       String
  status                      DesignProductSelectionStatus @default(SELECTED)
  errorMessage                String?
  createdAt                   DateTime                     @default(now())
  updatedAt                   DateTime                     @updatedAt

  design                  DesignAsset              @relation(fields: [designId], references: [id], onDelete: Cascade)
  localBaseProduct        BaseProduct?             @relation(fields: [localBaseProductId], references: [id])
  printfulProductTemplate PrintfulProductTemplate? @relation(fields: [printfulProductTemplateId], references: [id])
  placementPreset         PlacementPreset          @relation(fields: [placementPresetId], references: [id])
  mockupAssets            MockupAsset[]
  productListing          CommerceListing?

  @@unique([designId, pipeline, positionHash])
  @@index([designId, pipeline])
  @@index([status])
}

model MockupAsset {
  id                       String            @id @default(uuid())
  designId                 String
  designProductSelectionId String
  pipeline                 PipelineType
  provider                 ProviderType
  objectKey                String?
  contentType              String?
  format                   String?
  imageUrl                 String?
  thumbnailUrl             String?
  mockupType               MockupAssetType
  providerMockupId         String?
  providerTaskId           String?
  status                   MockupAssetStatus @default(PENDING)
  widthPx                  Int?
  heightPx                 Int?
  dpi                      Int?
  checksum                 String?
  placementSnapshotJson    Json?
  renderJobId              String?
  failureReason            String?
  replacedById             String?
  archivedAt               DateTime?
  metadataJson             Json?
  createdAt                DateTime          @default(now())
  updatedAt                DateTime          @updatedAt

  design           DesignAsset            @relation(fields: [designId], references: [id], onDelete: Cascade)
  productSelection DesignProductSelection @relation(fields: [designProductSelectionId], references: [id], onDelete: Cascade)

  @@index([designId, pipeline])
  @@index([designProductSelectionId, status])
  @@index([renderJobId])
}

model CommerceListing {
  id             String       @id @default(uuid())
  type           ListingType
  status         ListingStatus @default(DRAFT)
  designerId     String
  designAssetId  String
  pipeline       PipelineType?
  title          String
  description    String?
  tags           Json?
  categoryId     String?
  productType    String?
  localBaseProductId String?
  printfulProductTemplateId String?
  designProductSelectionId String? @unique
  mockupAssetIds Json?
  slug           String       @unique
  price          Decimal      @db.Decimal(10, 2)
  currency       String       @default("UZS")
  cost           Decimal?     @db.Decimal(10, 2)
  marketplaceFeeEstimate Decimal? @db.Decimal(10, 2)
  rashpodCommission Decimal? @db.Decimal(10, 2)
  designerRoyalty Decimal? @db.Decimal(10, 2)
  seoMetadata    Json?
  imagesJson     Json?
  metadataJson   Json?
  publishedAt    DateTime?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  designer    User       @relation("ListingDesigner", fields: [designerId], references: [id])
  designAsset DesignAsset @relation(fields: [designAssetId], references: [id])
  localBaseProduct BaseProduct? @relation(fields: [localBaseProductId], references: [id])
  printfulProductTemplate PrintfulProductTemplate? @relation(fields: [printfulProductTemplateId], references: [id])
  designProductSelection DesignProductSelection? @relation(fields: [designProductSelectionId], references: [id])
  marketplacePublications MarketplacePublication[]
  marketplaceExportItems MarketplaceExportItem[]
  exportedListings ExportedListing[]
  marketplaceExternalSales MarketplaceExternalSale[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]
  podSyncRecords PodProviderSyncRecord[]
  cartItems   CartItem[]
  orderItems  OrderItem[]
  royaltyLedgerEntries RoyaltyLedgerEntry[]
  supportRequests SupportRequest[]
}

model MarketplaceConfig {
  id                   String               @id @default(uuid())
  key                  ManualMarketplaceKey
  marketplaceType      MarketplaceType      @default(MANUAL_EXPORT)
  name                 String
  isEnabled            Boolean              @default(true)
  countryCode          String               @default("UZ")
  currency             String               @default("UZS")
  defaultLanguage      String               @default("uz-Latn")
  supportedLanguages   Json?
  supportedListingTypes Json?
  exportFormats        Json?
  imageRequirementsJson Json?
  pricingRulesJson     Json?
  skuPrefix            String               @default("RPD")
  statusMappingJson    Json?
  notes                String?
  metadataJson         Json?
  createdById          String?
  updatedById          String?
  createdAt            DateTime             @default(now())
  updatedAt            DateTime             @updatedAt

  categoryMappings MarketplaceCategoryMapping[]
  exportBatches    MarketplaceExportBatch[]
  exportedListings ExportedListing[]
  externalSales    MarketplaceExternalSale[]
  externalOrderImports ExternalOrderImport[]
  externalOrderIntakes ExternalOrderIntake[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]

  @@unique([key, name])
  @@index([key, isEnabled])
}

model MarketplaceCategoryMapping {
  id                         String   @id @default(uuid())
  marketplaceConfigId        String
  productTypeId              String
  baseProductId              String?
  marketplaceCategoryId      String
  marketplaceCategoryName    String
  requiredAttributesJson     Json?
  optionalAttributesJson     Json?
  valueMappingsJson          Json?
  isActive                   Boolean  @default(true)
  createdById                String?
  updatedById                String?
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt

  marketplaceConfig MarketplaceConfig @relation(fields: [marketplaceConfigId], references: [id], onDelete: Cascade)
  productType       ProductType       @relation(fields: [productTypeId], references: [id], onDelete: Restrict)
  baseProduct       BaseProduct?      @relation(fields: [baseProductId], references: [id], onDelete: SetNull)

  @@unique([marketplaceConfigId, productTypeId, baseProductId])
  @@index([marketplaceConfigId, isActive])
  @@index([productTypeId])
  @@index([baseProductId])
}

model MarketplaceExportBatch {
  id                    String                       @id @default(uuid())
  marketplaceConfigId   String
  status                MarketplaceExportBatchStatus @default(DRAFT)
  exportFormat          MarketplaceExportFormat      @default(CSV)
  selectedListingIdsJson Json?
  exportFileAssetId     String?
  zipFileAssetId        String?
  generatedAt           DateTime?
  exportedAt            DateTime?
  failureReason         String?
  notes                 String?
  createdById           String?
  updatedById           String?
  metadataJson          Json?
  createdAt             DateTime                     @default(now())
  updatedAt             DateTime                     @updatedAt

  marketplaceConfig MarketplaceConfig @relation(fields: [marketplaceConfigId], references: [id], onDelete: Restrict)
  exportFileAsset   FileAsset?        @relation("MarketplaceExportFile", fields: [exportFileAssetId], references: [id], onDelete: SetNull)
  zipFileAsset      FileAsset?        @relation("MarketplaceExportZipFile", fields: [zipFileAssetId], references: [id], onDelete: SetNull)
  items             MarketplaceExportItem[]
  exportedListings  ExportedListing[]

  @@index([marketplaceConfigId, status, createdAt])
  @@index([exportFileAssetId])
  @@index([zipFileAssetId])
}

model MarketplaceExportItem {
  id                    String                      @id @default(uuid())
  batchId               String
  listingId             String
  status                MarketplaceExportItemStatus @default(DRAFT)
  eligible              Boolean                     @default(false)
  blockersJson          Json?
  warningsJson          Json?
  exportSku             String
  contentSnapshotJson   Json?
  priceSnapshotJson     Json?
  categorySnapshotJson  Json?
  imageSnapshotJson     Json?
  variantSnapshotJson   Json?
  skuOverrideReason     String?
  priceOverrideReason   String?
  rowJson               Json?
  createdAt             DateTime                    @default(now())
  updatedAt             DateTime                    @updatedAt

  batch   MarketplaceExportBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)
  listing CommerceListing        @relation(fields: [listingId], references: [id], onDelete: Cascade)

  @@unique([batchId, listingId])
  @@index([listingId])
  @@index([exportSku])
  @@index([status, eligible])
}

model ExportedListing {
  id                    String                @id @default(uuid())
  marketplaceConfigId   String
  listingId             String
  exportBatchId         String?
  exportSku             String
  exportTitle           String
  exportPrice           Decimal               @db.Decimal(10, 2)
  currency              String                @default("UZS")
  categorySnapshotJson  Json?
  imageSnapshotJson     Json?
  contentSnapshotJson   Json?
  priceSnapshotJson     Json?
  status                ExportedListingStatus @default(EXPORTED)
  externalUrl           String?
  externalListingId     String?
  manuallyListedById    String?
  manuallyListedAt      DateTime?
  lastCheckedAt         DateTime?
  notes                 String?
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt

  marketplaceConfig MarketplaceConfig       @relation(fields: [marketplaceConfigId], references: [id], onDelete: Restrict)
  listing           CommerceListing         @relation(fields: [listingId], references: [id], onDelete: Cascade)
  exportBatch       MarketplaceExportBatch? @relation(fields: [exportBatchId], references: [id], onDelete: SetNull)
  externalSales     MarketplaceExternalSale[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]

  @@unique([marketplaceConfigId, exportSku])
  @@index([listingId])
  @@index([status, updatedAt])
  @@index([externalListingId])
}

model MarketplaceExternalSale {
  id                     String                          @id @default(uuid())
  marketplaceConfigId    String
  exportedListingId      String?
  listingId              String
  exportSku              String
  externalOrderId        String?
  quantity               Int                             @default(1)
  salePrice              Decimal                         @db.Decimal(10, 2)
  currency               String                          @default("UZS")
  customerSnapshotJson   Json?
  deliverySnapshotJson   Json?
  saleSnapshotJson       Json?
  conversionSnapshotJson Json?
  source                 MarketplaceExternalSaleSource   @default(MANUAL)
  status                 MarketplaceExternalSaleStatus   @default(RECORDED)
  notes                  String?
  createdById            String?
  createdAt              DateTime                        @default(now())
  updatedAt              DateTime                        @updatedAt

  marketplaceConfig MarketplaceConfig @relation(fields: [marketplaceConfigId], references: [id], onDelete: Restrict)
  exportedListing   ExportedListing?  @relation(fields: [exportedListingId], references: [id], onDelete: SetNull)
  listing           CommerceListing   @relation(fields: [listingId], references: [id], onDelete: Cascade)

  @@index([marketplaceConfigId, status, createdAt])
  @@index([exportedListingId])
  @@index([listingId])
  @@index([externalOrderId])
}

model ExternalOrderImport {
  id                     String                    @id @default(uuid())
  sourceType             ExternalOrderSourceType
  marketplaceConfigId    String?
  fileAssetId            String?
  originalFilename       String?
  mimeType               String?
  status                 ExternalOrderImportStatus @default(UPLOADED)
  columnMappingJson      Json?
  parsedRowsJson         Json?
  validationErrorsJson   Json?
  validationWarningsJson Json?
  importedCount          Int                       @default(0)
  failedCount            Int                       @default(0)
  strictMode             Boolean                   @default(false)
  rawPayloadHash         String?
  notes                  String?
  createdById            String?
  createdAt              DateTime                  @default(now())
  updatedAt              DateTime                  @updatedAt

  marketplaceConfig MarketplaceConfig? @relation(fields: [marketplaceConfigId], references: [id], onDelete: SetNull)
  fileAsset         FileAsset?         @relation(fields: [fileAssetId], references: [id], onDelete: SetNull)
  createdBy         User?              @relation("ExternalOrderImportCreator", fields: [createdById], references: [id], onDelete: SetNull)
  intakes           ExternalOrderIntake[]

  @@index([sourceType, status, createdAt])
  @@index([marketplaceConfigId])
  @@index([fileAssetId])
  @@index([rawPayloadHash])
}

model ExternalOrderIntake {
  id                     String                       @id @default(uuid())
  sourceType             ExternalOrderSourceType
  marketplaceConfigId    String?
  importId               String?
  externalOrderId        String?
  externalOrderUrl       String?
  customerName           String
  customerPhone          String
  customerEmail          String?
  deliveryMethod         String?
  deliveryAddress        String?
  pickupLocation         String?
  paymentStatus          ExternalOrderPaymentStatus   @default(MANUAL_REVIEW)
  currency               String                       @default("UZS")
  totalAmount            Decimal                      @db.Decimal(12, 2)
  notes                  String?
  rawPayloadJson         Json?
  rawPayloadHash         String?
  status                 ExternalOrderIntakeStatus    @default(DRAFT)
  duplicateStatus        ExternalOrderDuplicateStatus @default(UNIQUE)
  validationErrorsJson   Json?
  validationWarningsJson Json?
  conversionPreviewJson  Json?
  internalOrderId        String?                      @unique
  convertedAt            DateTime?
  canceledAt             DateTime?
  createdById            String?
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt

  marketplaceConfig   MarketplaceConfig? @relation(fields: [marketplaceConfigId], references: [id], onDelete: SetNull)
  import              ExternalOrderImport? @relation(fields: [importId], references: [id], onDelete: SetNull)
  internalOrder       Order?             @relation("ExternalOrderInternalOrder", fields: [internalOrderId], references: [id], onDelete: SetNull)
  createdBy           User?              @relation("ExternalOrderIntakeCreator", fields: [createdById], references: [id], onDelete: SetNull)
  items               ExternalOrderIntakeItem[]
  duplicateCandidates ExternalOrderDuplicateCandidate[] @relation("ExternalOrderDuplicateSource")
  duplicateMatches    ExternalOrderDuplicateCandidate[] @relation("ExternalOrderDuplicateMatch")

  @@index([sourceType, status, createdAt])
  @@index([marketplaceConfigId, externalOrderId])
  @@index([externalOrderId])
  @@index([customerPhone, totalAmount, createdAt])
  @@index([rawPayloadHash])
  @@index([duplicateStatus])
}

model ExternalOrderIntakeItem {
  id                     String                         @id @default(uuid())
  intakeId               String
  externalSku            String?
  externalListingId      String?
  externalListingUrl     String?
  marketplaceConfigId    String?
  exportedListingId      String?
  listingId              String?
  baseProductId          String?
  productTypeId          String?
  designId               String?
  designVersionId        String?
  providerSyncRecordId   String?
  title                  String
  selectedSize           String?
  selectedColor          String?
  selectedMaterial       String?
  selectedPrintSide      String?
  quantity               Int                            @default(1)
  unitPrice              Decimal                        @db.Decimal(12, 2)
  currency               String                         @default("UZS")
  imageUrl               String?
  previewAssetId         String?
  mappingStatus          ExternalOrderItemMappingStatus @default(NEEDS_MAPPING)
  validationErrorsJson   Json?
  validationWarningsJson Json?
  matchedSnapshotJson    Json?
  rawItemJson            Json?
  createdAt              DateTime                       @default(now())
  updatedAt              DateTime                       @updatedAt

  intake             ExternalOrderIntake @relation(fields: [intakeId], references: [id], onDelete: Cascade)
  marketplaceConfig  MarketplaceConfig?  @relation(fields: [marketplaceConfigId], references: [id], onDelete: SetNull)
  exportedListing    ExportedListing?    @relation(fields: [exportedListingId], references: [id], onDelete: SetNull)
  listing            CommerceListing?    @relation(fields: [listingId], references: [id], onDelete: SetNull)
  baseProduct        BaseProduct?        @relation(fields: [baseProductId], references: [id], onDelete: SetNull)
  productType        ProductType?        @relation(fields: [productTypeId], references: [id], onDelete: SetNull)
  design             DesignAsset?        @relation(fields: [designId], references: [id], onDelete: SetNull)
  designVersion      DesignVersion?      @relation(fields: [designVersionId], references: [id], onDelete: SetNull)
  providerSyncRecord PodProviderSyncRecord? @relation(fields: [providerSyncRecordId], references: [id], onDelete: SetNull)
  previewAsset       FileAsset?          @relation(fields: [previewAssetId], references: [id], onDelete: SetNull)
  orderItems         OrderItem[]

  @@index([intakeId])
  @@index([externalSku])
  @@index([exportedListingId])
  @@index([listingId])
  @@index([mappingStatus])
  @@index([providerSyncRecordId])
}

model ExternalOrderDuplicateCandidate {
  id               String                       @id @default(uuid())
  intakeId         String
  matchedIntakeId  String?
  duplicateStatus  ExternalOrderDuplicateStatus @default(POSSIBLE_DUPLICATE)
  matchType        String
  score            Decimal                      @default(0) @db.Decimal(5, 2)
  detailsJson      Json?
  resolutionReason String?
  resolvedById     String?
  resolvedAt       DateTime?
  createdAt        DateTime                     @default(now())
  updatedAt        DateTime                     @updatedAt

  intake        ExternalOrderIntake  @relation("ExternalOrderDuplicateSource", fields: [intakeId], references: [id], onDelete: Cascade)
  matchedIntake ExternalOrderIntake? @relation("ExternalOrderDuplicateMatch", fields: [matchedIntakeId], references: [id], onDelete: SetNull)

  @@index([intakeId, duplicateStatus])
  @@index([matchedIntakeId])
  @@index([matchType])
}

model MarketplacePublication {
  id                         String                       @id @default(uuid())
  productListingId           String
  marketplace                MarketplaceKind
  provider                   ProviderType
  providerStoreId            String?
  providerSyncProductId      String?
  providerExternalListingId  String?
  status                     MarketplacePublicationStatus @default(DRAFT)
  errorMessage               String?
  lastSyncedAt               DateTime?
  metadataJson               Json?
  createdAt                  DateTime                     @default(now())
  updatedAt                  DateTime                     @updatedAt

  productListing CommerceListing @relation(fields: [productListingId], references: [id], onDelete: Cascade)

  @@unique([productListingId, marketplace])
  @@index([marketplace, status])
}

model PrintfulFileMapping {
  id              String                @id @default(uuid())
  designId        String
  printfulFileId  String?
  originalUrl     String?
  printfulUrl     String?
  status          PrintfulMappingStatus @default(PENDING)
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt

  design DesignAsset @relation(fields: [designId], references: [id], onDelete: Cascade)

  @@unique([designId, printfulFileId])
  @@index([designId, status])
}

model PodProviderConfig {
  id                         String               @id @default(uuid())
  provider                   PodProviderType
  mode                       PodProviderMode      @default(TEST)
  displayName                String
  isEnabled                  Boolean              @default(false)
  credentialEnvVar           String?
  credentialSecretRef        String?
  webhookSecretEnvVar        String?
  webhookSecretRef           String?
  apiBaseUrl                 String?
  storeId                    String?
  defaultCurrency            String               @default("USD")
  defaultCountryCode         String?
  defaultRegion              String?
  fulfillmentRegion          String?
  sellingRegion              String?
  shippingPreference         String?
  lastCatalogSyncStatus      PodCatalogSyncStatus?
  lastCatalogSyncAt          DateTime?
  lastCatalogSyncError       String?
  metadataJson               Json?
  createdById                String?
  updatedById                String?
  createdAt                  DateTime             @default(now())
  updatedAt                  DateTime             @updatedAt

  catalogSyncRuns PodProviderCatalogSyncRun[]
  products        PodProviderProduct[]
  productMappings PodProductMapping[]
  printAreaMappings PodPrintAreaMapping[]
  providerFiles   PodProviderFile[]
  syncRecords     PodProviderSyncRecord[]
  webhookEvents   PodWebhookEvent[]

  @@unique([provider, mode])
  @@index([provider, mode, isEnabled])
}

model PodProviderCatalogSyncRun {
  id                  String               @id @default(uuid())
  providerConfigId    String
  provider            PodProviderType
  mode                PodProviderMode
  status              PodCatalogSyncStatus @default(PENDING)
  productsSeen        Int                  @default(0)
  productsUpserted    Int                  @default(0)
  variantsSeen        Int                  @default(0)
  printAreasSeen      Int                  @default(0)
  unavailableMarked   Int                  @default(0)
  errorMessage        String?
  requestedById       String?
  startedAt           DateTime?
  completedAt         DateTime?
  metadataJson        Json?
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt

  providerConfig PodProviderConfig @relation(fields: [providerConfigId], references: [id], onDelete: Cascade)

  @@index([providerConfigId, status, createdAt])
}

model PodProviderProduct {
  id                   String                @id @default(uuid())
  providerConfigId     String
  provider             PodProviderType
  mode                 PodProviderMode
  providerProductId    String
  name                 String
  category             String?
  brand                String?
  model                String?
  description          String?
  imageUrlsJson        Json?
  availabilityStatus   PodAvailabilityStatus @default(AVAILABLE)
  regionsJson          Json?
  currency             String                @default("USD")
  baseCost             Decimal?              @db.Decimal(10, 2)
  suggestedRetailPrice Decimal?              @db.Decimal(10, 2)
  rawMetadataJson      Json?
  lastSyncedAt         DateTime?
  archivedAt           DateTime?
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt

  providerConfig    PodProviderConfig      @relation(fields: [providerConfigId], references: [id], onDelete: Cascade)
  variants          PodProviderVariant[]
  printAreas        PodProviderPrintArea[]
  productMappings   PodProductMapping[]
  printAreaMappings PodPrintAreaMapping[]
  syncRecords       PodProviderSyncRecord[]

  @@unique([providerConfigId, providerProductId])
  @@index([provider, mode, availabilityStatus])
}

model PodProviderVariant {
  id                   String                @id @default(uuid())
  providerProductId    String
  providerVariantId    String
  name                 String
  sku                  String?
  color                String?
  size                 String?
  material             String?
  availabilityStatus   PodAvailabilityStatus @default(AVAILABLE)
  regionsJson          Json?
  currency             String                @default("USD")
  baseCost             Decimal?              @db.Decimal(10, 2)
  suggestedRetailPrice Decimal?              @db.Decimal(10, 2)
  rawMetadataJson      Json?
  lastSyncedAt         DateTime?
  archivedAt           DateTime?
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt

  providerProduct PodProviderProduct @relation(fields: [providerProductId], references: [id], onDelete: Cascade)

  @@unique([providerProductId, providerVariantId])
  @@index([availabilityStatus])
}

model PodProviderPrintArea {
  id                   String                @id @default(uuid())
  providerProductId    String
  providerPrintAreaId  String
  name                 String
  placement            String?
  width                Float?
  height               Float?
  units                PlacementUnits        @default(INCH)
  dpi                  Int?
  techniquesJson       Json?
  availabilityStatus   PodAvailabilityStatus @default(AVAILABLE)
  rawMetadataJson      Json?
  lastSyncedAt         DateTime?
  archivedAt           DateTime?
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt

  providerProduct   PodProviderProduct    @relation(fields: [providerProductId], references: [id], onDelete: Cascade)
  printAreaMappings PodPrintAreaMapping[]

  @@unique([providerProductId, providerPrintAreaId])
  @@index([placement, availabilityStatus])
}

model PodProductMapping {
  id                       String            @id @default(uuid())
  providerConfigId         String
  productTypeId            String
  baseProductId            String?
  providerProductId        String
  providerVariantIdsJson   Json?
  defaultProviderVariantId String?
  colorMappingJson         Json?
  sizeMappingJson          Json?
  materialMappingJson      Json?
  region                   String?
  currency                 String            @default("USD")
  isActive                 Boolean           @default(true)
  quality                  PodMappingQuality @default(MANUAL_REVIEW)
  notes                    String?
  metadataJson             Json?
  createdById              String?
  updatedById              String?
  createdAt                DateTime          @default(now())
  updatedAt                DateTime          @updatedAt

  providerConfig PodProviderConfig  @relation(fields: [providerConfigId], references: [id], onDelete: Cascade)
  productType    ProductType        @relation(fields: [productTypeId], references: [id], onDelete: Restrict)
  baseProduct    BaseProduct?       @relation(fields: [baseProductId], references: [id], onDelete: SetNull)
  providerProduct PodProviderProduct @relation(fields: [providerProductId], references: [id], onDelete: Restrict)
  syncRecords    PodProviderSyncRecord[]

  @@unique([providerConfigId, productTypeId, baseProductId, providerProductId])
  @@index([providerConfigId, isActive])
  @@index([productTypeId])
  @@index([baseProductId])
}

model PodPrintAreaMapping {
  id                         String            @id @default(uuid())
  providerConfigId           String
  printAreaId                String
  providerProductId          String
  providerPrintAreaId        String
  coordinateSystem           String            @default("PROVIDER_UNITS")
  providerPlacement          String?
  providerUnits              PlacementUnits    @default(INCH)
  providerDpi                Int?
  providerWidth              Float?
  providerHeight             Float?
  offsetX                    Float             @default(0)
  offsetY                    Float             @default(0)
  supportsRotation           Boolean           @default(false)
  minScale                   Float?
  maxScale                   Float?
  safeZoneMappingJson        Json?
  technique                  String?
  isActive                   Boolean           @default(true)
  quality                    PodMappingQuality @default(MANUAL_REVIEW)
  notes                      String?
  metadataJson               Json?
  createdById                String?
  updatedById                String?
  createdAt                  DateTime          @default(now())
  updatedAt                  DateTime          @updatedAt

  providerConfig   PodProviderConfig    @relation(fields: [providerConfigId], references: [id], onDelete: Cascade)
  printArea        PrintArea            @relation(fields: [printAreaId], references: [id], onDelete: Restrict)
  providerProduct  PodProviderProduct   @relation(fields: [providerProductId], references: [id], onDelete: Restrict)
  providerPrintArea PodProviderPrintArea @relation(fields: [providerPrintAreaId], references: [id], onDelete: Restrict)
  syncRecords      PodProviderSyncRecord[]

  @@unique([providerConfigId, printAreaId, providerProductId, providerPrintAreaId])
  @@index([providerConfigId, isActive])
  @@index([printAreaId])
}

model PodProviderFile {
  id                  String                @id @default(uuid())
  providerConfigId    String
  provider            PodProviderType
  mode                PodProviderMode
  sourceAssetId       String
  designId            String?
  designVersionId     String?
  providerFileId      String?
  transferUrlSnapshot String?
  status              PodProviderFileStatus @default(PENDING)
  failureReason       String?
  uploadedAt          DateTime?
  expiresAt           DateTime?
  metadataJson        Json?
  createdAt           DateTime              @default(now())
  updatedAt           DateTime              @updatedAt

  providerConfig PodProviderConfig @relation(fields: [providerConfigId], references: [id], onDelete: Cascade)
  sourceAsset    FileAsset         @relation(fields: [sourceAssetId], references: [id], onDelete: Restrict)
  syncRecords    PodProviderSyncRecord[]

  @@unique([providerConfigId, sourceAssetId])
  @@index([provider, mode, status])
  @@index([sourceAssetId])
}

model PodProviderSyncRecord {
  id                                String              @id @default(uuid())
  providerConfigId                  String
  provider                          PodProviderType
  mode                              PodProviderMode
  listingId                         String
  designId                          String
  designVersionId                   String?
  productTypeId                     String?
  baseProductId                     String?
  productMappingId                  String
  printAreaMappingId                String?
  providerProductId                 String
  providerVariantId                 String?
  providerFileId                    String?
  providerDraftProductId            String?
  providerExternalProductId         String?
  status                            PodSyncRecordStatus @default(DRAFT)
  failureReason                     String?
  placementHash                     String
  providerPlacementPayloadSnapshotJson Json?
  payloadSnapshotJson               Json?
  responseSnapshotJson              Json?
  providerMockupSnapshotJson        Json?
  createdById                       String?
  syncedAt                          DateTime?
  archivedAt                        DateTime?
  replacedBySyncRecordId            String?
  createdAt                         DateTime            @default(now())
  updatedAt                         DateTime            @updatedAt

  providerConfig   PodProviderConfig    @relation(fields: [providerConfigId], references: [id], onDelete: Cascade)
  listing          CommerceListing      @relation(fields: [listingId], references: [id], onDelete: Cascade)
  providerProduct  PodProviderProduct   @relation(fields: [providerProductId], references: [id], onDelete: Restrict)
  productMapping   PodProductMapping    @relation(fields: [productMappingId], references: [id], onDelete: Restrict)
  printAreaMapping PodPrintAreaMapping? @relation(fields: [printAreaMappingId], references: [id], onDelete: SetNull)
  providerFile     PodProviderFile?     @relation(fields: [providerFileId], references: [id], onDelete: SetNull)
  mockups          PodProviderMockup[]
  orderItems       OrderItem[]
  productionJobs   ProductionJob[]
  externalOrderIntakeItems ExternalOrderIntakeItem[]

  @@unique([providerConfigId, listingId, productMappingId, placementHash])
  @@index([providerConfigId, status, updatedAt])
  @@index([listingId])
  @@index([designId])
}

model PodProviderMockup {
  id                    String          @id @default(uuid())
  syncRecordId          String
  providerMockupId      String?
  kind                  MockupAssetType
  imageUrl              String?
  thumbnailUrl          String?
  widthPx               Int?
  heightPx              Int?
  status                PodMockupStatus @default(PENDING)
  payloadSnapshotJson   Json?
  failureReason         String?
  createdAt             DateTime        @default(now())
  updatedAt             DateTime        @updatedAt

  syncRecord PodProviderSyncRecord @relation(fields: [syncRecordId], references: [id], onDelete: Cascade)

  @@index([syncRecordId, status])
}

model PodWebhookEvent {
  id                  String                @id @default(uuid())
  providerConfigId    String?
  provider            PodProviderType
  mode                PodProviderMode?
  eventType           String?
  signatureValid      Boolean?
  status              PodWebhookEventStatus @default(ACCEPTED)
  payloadSummaryJson  Json?
  rawPayloadJson      Json?
  errorMessage        String?
  receivedAt          DateTime              @default(now())
  processedAt         DateTime?
  createdAt           DateTime              @default(now())

  providerConfig PodProviderConfig? @relation(fields: [providerConfigId], references: [id], onDelete: SetNull)

  @@index([provider, receivedAt])
  @@index([providerConfigId, status])
}

model IntegrationLog {
  id                         String               @id @default(uuid())
  designId                   String?
  productListingId           String?
  marketplacePublicationId   String?
  action                     String
  requestId                  String?
  status                     IntegrationLogStatus
  errorCode                  String?
  errorMessage               String?
  responseSummaryJson        Json?
  createdAt                  DateTime             @default(now())

  @@index([designId, createdAt])
  @@index([productListingId, createdAt])
  @@index([marketplacePublicationId, createdAt])
  @@index([action, status])
}

model Cart {
  id         String   @id @default(uuid())
  customerId String   @unique
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  items      CartItem[]
}

model CartItem {
  id         String   @id @default(uuid())
  cartId     String
  listingId  String?
  quantity   Int
  unitPrice  Decimal  @db.Decimal(10, 2)
  currency   String   @default("UZS")
  selectedSize String?
  selectedColor String?
  selectedMaterial String?
  selectedPrintSide String?
  itemKind   FilmOrderKind?
  filmType   FilmType?
  filmWidthCm Float?
  filmHeightCm Float?
  filmAreaCm2 Float?
  filmSourceAssetId String?
  filmPricingSnapshotJson Json?
  filmConsentSnapshotJson Json?
  filmOptionsJson Json?
  metadataJson Json?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  cart       Cart            @relation(fields: [cartId], references: [id], onDelete: Cascade)
  listing    CommerceListing? @relation(fields: [listingId], references: [id])
}

model Order {
  id            String      @id @default(uuid())
  customerId    String
  status        OrderStatus @default(PENDING_PAYMENT)
  customerName  String?
  customerPhone String?
  customerEmail String?
  deliveryAddress String?
  pickupLocation String?
  customerNote  String?
  subtotal      Decimal     @db.Decimal(10, 2)
  deliveryFee   Decimal     @db.Decimal(10, 2)
  discountTotal Decimal     @default(0) @db.Decimal(10, 2)
  total         Decimal     @db.Decimal(10, 2)
  currency      String      @default("UZS")
  deliveryType  String?
  deliveryZone  String?
  notes         String?
  source        String                  @default("RASHPOD")
  channel       String?
  externalSourceType ExternalOrderSourceType?
  externalOrderId String?
  externalSourceSnapshotJson Json?
  pricingSnapshotJson Json?
  deliverySnapshotJson Json?
  paymentSnapshotJson Json?
  fulfillmentRoute OrderFulfillmentRoute @default(LOCAL_PRODUCTION)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  customer      User                 @relation("CustomerOrders", fields: [customerId], references: [id])
  items         OrderItem[]
  payments      PaymentTransaction[]
  productionJobs ProductionJob[]
  ledgerEntries LedgerEntry[]
  royaltyLedgerEntries RoyaltyLedgerEntry[]
  financeSnapshots OrderFinanceSnapshot[]
  paymentReconciliations PaymentReconciliation[]
  supportRequests SupportRequest[]
  externalOrderIntake ExternalOrderIntake? @relation("ExternalOrderInternalOrder")

  @@index([source, channel, createdAt])
  @@index([externalSourceType, externalOrderId])
}

model SupportRequest {
  id            String               @id @default(uuid())
  requesterId   String
  requesterRole UserRole
  orderId       String?
  designAssetId String?
  listingId     String?
  category      String
  subject       String?
  message       String
  status        SupportRequestStatus @default(OPEN)
  reviewNotes   String?
  closedAt      DateTime?
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt

  requester User             @relation("SupportRequestRequester", fields: [requesterId], references: [id], onDelete: Cascade)
  order     Order?           @relation(fields: [orderId], references: [id], onDelete: SetNull)
  design    DesignAsset?     @relation(fields: [designAssetId], references: [id], onDelete: SetNull)
  listing   CommerceListing? @relation(fields: [listingId], references: [id], onDelete: SetNull)

  @@index([requesterId, status, createdAt])
  @@index([orderId])
  @@index([designAssetId])
  @@index([listingId])
}

model OrderItem {
  id          String   @id @default(uuid())
  orderId     String
  listingId   String?
  listingTitle String?
  designerId  String?
  designAssetId String?
  designVersionId String?
  productTypeId String?
  productTypeName String?
  baseProductId String?
  baseProductName String?
  baseProductSku String?
  selectedSize String?
  selectedColor String?
  selectedMaterial String?
  selectedPrintSide String?
  itemKind   FilmOrderKind?
  filmType   FilmType?
  filmWidthCm Float?
  filmHeightCm Float?
  filmAreaCm2 Float?
  filmSourceAssetId String?
  filmPricingSnapshotJson Json?
  filmConsentSnapshotJson Json?
  filmProductionSnapshotJson Json?
  mockupAssetIds Json?
  mockupImageUrl String?
  productionFileAssetId String?
  placementSnapshotJson Json?
  printAreaSnapshotJson Json?
  royaltySnapshotJson Json?
  pricingSnapshotJson Json?
  productionSnapshotJson Json?
  fulfillmentRoute OrderFulfillmentRoute @default(LOCAL_PRODUCTION)
  providerSyncRecordId String?
  providerType PodProviderType?
  providerProductId String?
  providerVariantId String?
  providerFileId String?
  providerPlacementPayloadSnapshotJson Json?
  providerFulfillmentSnapshotJson Json?
  externalOrderIntakeItemId String?
  externalSourceType ExternalOrderSourceType?
  externalOrderId String?
  externalSku String?
  externalListingId String?
  externalChannelSnapshotJson Json?
  quantity    Int
  unitPrice   Decimal  @db.Decimal(10, 2)
  totalPrice  Decimal  @db.Decimal(10, 2)
  designerRoyaltyAmount Decimal? @db.Decimal(10, 2)
  productionCostEstimate Decimal? @db.Decimal(10, 2)
  deliveryFeeAllocation Decimal? @db.Decimal(10, 2)
  metadataJson Json?
  createdAt   DateTime @default(now())

  order       Order           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  listing     CommerceListing? @relation(fields: [listingId], references: [id])
  providerSyncRecord PodProviderSyncRecord? @relation(fields: [providerSyncRecordId], references: [id], onDelete: SetNull)
  externalOrderIntakeItem ExternalOrderIntakeItem? @relation(fields: [externalOrderIntakeItemId], references: [id], onDelete: SetNull)
  productionJobs ProductionJob[]
  ledgerEntries LedgerEntry[]
  royaltyLedgerEntries RoyaltyLedgerEntry[]
  financeSnapshots OrderFinanceSnapshot[]

  @@index([externalOrderIntakeItemId])
  @@index([externalSourceType, externalOrderId])
}

model PaymentTransaction {
  id               String        @id @default(uuid())
  orderId           String
  provider          String
  providerRef       String?
  providerPaymentId String?
  checkoutUrl       String?
  attemptNumber     Int           @default(1)
  amount            Decimal       @db.Decimal(10, 2)
  currency          String        @default("UZS")
  status            PaymentStatus @default(PENDING)
  rawPayloadJson    Json?
  settingsSnapshotJson Json?
  idempotencyKey    String?
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt

  order             Order @relation(fields: [orderId], references: [id], onDelete: Cascade)
  ledgerEntries     LedgerEntry[]
  reconciliations   PaymentReconciliation[]
}

model OrderFinanceSnapshot {
  id                    String   @id @default(uuid())
  orderId               String
  orderItemId           String
  listingId             String?
  designerId            String?
  customerId            String?
  currency              String   @default("UZS")
  unitPrice             Decimal  @db.Decimal(12, 2)
  quantity              Int
  grossLineAmount       Decimal  @db.Decimal(12, 2)
  discountAmount        Decimal  @default(0) @db.Decimal(12, 2)
  netRevenue            Decimal  @db.Decimal(12, 2)
  deliveryFeeAllocation Decimal? @db.Decimal(12, 2)
  taxAmount             Decimal? @db.Decimal(12, 2)
  paymentProviderFee    Decimal? @db.Decimal(12, 2)
  baseProductCost       Decimal? @db.Decimal(12, 2)
  productionCost        Decimal? @db.Decimal(12, 2)
  deliveryCost          Decimal? @db.Decimal(12, 2)
  royaltyRuleSnapshot   Json?
  royaltyAmount         Decimal  @default(0) @db.Decimal(12, 2)
  platformMarginEstimate Decimal? @db.Decimal(12, 2)
  marginIncomplete      Boolean  @default(false)
  incompleteReason      String?
  settlementStatus      String   @default("PENDING")
  refundStatus          String   @default("NONE")
  calculationBasisJson  Json?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  order     Order     @relation(fields: [orderId], references: [id], onDelete: Cascade)
  orderItem OrderItem @relation(fields: [orderItemId], references: [id], onDelete: Cascade)

  @@unique([orderItemId])
  @@index([designerId, settlementStatus])
  @@index([orderId])
}

model CurrencyConfig {
  code              String   @id
  name              String
  symbol            String
  decimalPlaces     Int      @default(2)
  isActive          Boolean  @default(true)
  isPrimary         Boolean  @default(false)
  exchangeRateToUzs Decimal  @db.Decimal(18, 6)
  metadataJson      Json?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

model ExchangeRate {
  id           String             @id @default(uuid())
  fromCurrency String
  toCurrency   String             @default("UZS")
  rate         Decimal            @db.Decimal(18, 6)
  source       ExchangeRateSource @default(MANUAL)
  effectiveAt  DateTime
  createdAt    DateTime           @default(now())

  @@index([fromCurrency, toCurrency, effectiveAt])
}

model LedgerEntry {
  id                   String            @id @default(uuid())
  type                 LedgerEntryType
  status               LedgerEntryStatus @default(POSTED)
  designerId           String?
  orderId              String?
  orderItemId          String?
  paymentTransactionId String?
  amount               Decimal           @db.Decimal(12, 2)
  currency             String            @default("UZS")
  amountUzs            Decimal           @db.Decimal(12, 2)
  description          String?
  metadataJson         Json?
  postedAt             DateTime?
  createdAt            DateTime          @default(now())

  designer           User?               @relation("DesignerLedgerEntries", fields: [designerId], references: [id])
  order              Order?              @relation(fields: [orderId], references: [id], onDelete: SetNull)
  orderItem          OrderItem?          @relation(fields: [orderItemId], references: [id], onDelete: SetNull)
  paymentTransaction PaymentTransaction? @relation(fields: [paymentTransactionId], references: [id], onDelete: SetNull)
  payoutLines        PayoutLedgerEntry[]
  bonus              DesignerBonus?

  @@index([designerId, createdAt])
  @@index([type, status])
}

model RoyaltyLedgerEntry {
  id             String              @id @default(uuid())
  designerId     String
  orderId        String?
  orderItemId    String?
  listingId      String?
  designId       String?
  designVersionId String?
  entryType      RoyaltyEntryType    @default(PENDING)
  royaltyRuleId  String?
  royaltyRate    Decimal?            @db.Decimal(10, 4)
  amount         Decimal             @db.Decimal(12, 2)
  currency       String              @default("UZS")
  amountUzs      Decimal             @db.Decimal(12, 2)
  calculationBasis Json?
  royaltyRuleSnapshot Json?
  status         RoyaltyLedgerStatus @default(PENDING)
  availableAt    DateTime?
  reason         String?
  note           String?
  createdById    String?
  source         String?
  reversalOfEntryId String?
  metadataJson   Json?
  createdAt      DateTime            @default(now())
  acknowledgedAt DateTime?
  paidOutAt      DateTime?
  payoutId       String?

  designer    User            @relation("DesignerRoyaltyLedgerEntries", fields: [designerId], references: [id])
  order       Order?          @relation(fields: [orderId], references: [id], onDelete: Cascade)
  orderItem   OrderItem?      @relation(fields: [orderItemId], references: [id], onDelete: Cascade)
  listing     CommerceListing? @relation(fields: [listingId], references: [id])
  royaltyRule RoyaltyRule?    @relation(fields: [royaltyRuleId], references: [id], onDelete: SetNull)
  payout      Payout?         @relation(fields: [payoutId], references: [id], onDelete: SetNull)

  @@index([orderItemId, designerId])
  @@index([designerId, status])
  @@index([designerId, entryType])
  @@index([orderId])
}

model Payout {
  id            String       @id @default(uuid())
  designerId    String
  amount        Decimal      @db.Decimal(12, 2)
  currency      String       @default("UZS")
  amountUzs     Decimal      @db.Decimal(12, 2)
  method        PayoutMethod @default(MANUAL)
  status        PayoutStatus @default(DRAFT)
  reference     String?
  failureReason String?
  note          String?
  approvedById  String?
  paidById      String?
  exportFileRef String?
  metadataJson  Json?
  requestedAt   DateTime     @default(now())
  approvedAt    DateTime?
  processingAt  DateTime?
  confirmedAt   DateTime?
  paidAt        DateTime?
  canceledAt    DateTime?
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  designer       User                 @relation("DesignerPayouts", fields: [designerId], references: [id])
  royaltyEntries RoyaltyLedgerEntry[]
  ledgerEntries  PayoutLedgerEntry[]

  @@index([designerId, status])
}

model PaymentReconciliation {
  id                   String                      @id @default(uuid())
  paymentTransactionId String
  orderId              String
  provider             String
  providerTransactionId String?
  expectedAmount       Decimal                     @db.Decimal(12, 2)
  receivedAmount       Decimal?                    @db.Decimal(12, 2)
  currency             String                      @default("UZS")
  providerStatus       String?
  internalStatus       String?
  discrepancyAmount    Decimal                     @default(0) @db.Decimal(12, 2)
  status               PaymentReconciliationStatus @default(UNRECONCILED)
  reconciledAt         DateTime?
  reviewedById         String?
  note                 String?
  rawPayloadJson       Json?
  createdAt            DateTime                    @default(now())
  updatedAt            DateTime                    @updatedAt

  paymentTransaction PaymentTransaction @relation(fields: [paymentTransactionId], references: [id], onDelete: Cascade)
  order              Order              @relation(fields: [orderId], references: [id], onDelete: Cascade)

  @@unique([paymentTransactionId])
  @@index([status, createdAt])
  @@index([orderId])
}

model PayoutLedgerEntry {
  id            String   @id @default(uuid())
  payoutId      String
  ledgerEntryId String
  amount        Decimal  @db.Decimal(12, 2)
  currency      String   @default("UZS")
  amountUzs     Decimal  @db.Decimal(12, 2)
  createdAt     DateTime @default(now())

  payout      Payout      @relation(fields: [payoutId], references: [id], onDelete: Cascade)
  ledgerEntry LedgerEntry @relation(fields: [ledgerEntryId], references: [id], onDelete: Cascade)

  @@unique([payoutId, ledgerEntryId])
}

model DesignerBonus {
  id            String      @id @default(uuid())
  designerId    String
  groupBonusId  String?
  ledgerEntryId String?     @unique
  createdById   String?
  type          BonusType   @default(MANUAL)
  status        BonusStatus @default(APPLIED)
  amount        Decimal     @db.Decimal(12, 2)
  currency      String      @default("UZS")
  amountUzs     Decimal     @db.Decimal(12, 2)
  reason        String
  metadataJson  Json?
  createdAt     DateTime    @default(now())
  appliedAt     DateTime?
  cancelledAt   DateTime?

  designer    User         @relation("DesignerBonuses", fields: [designerId], references: [id])
  createdBy   User?        @relation("BonusCreator", fields: [createdById], references: [id], onDelete: SetNull)
  groupBonus  GroupBonus?  @relation(fields: [groupBonusId], references: [id], onDelete: SetNull)
  ledgerEntry LedgerEntry? @relation(fields: [ledgerEntryId], references: [id], onDelete: SetNull)

  @@index([designerId, status])
  @@index([groupBonusId])
}

model GroupBonus {
  id                   String      @id @default(uuid())
  name                 String
  type                 BonusType   @default(GROUP)
  status               BonusStatus @default(DRAFT)
  amountPerDesigner    Decimal     @db.Decimal(12, 2)
  currency             String      @default("UZS")
  amountPerDesignerUzs Decimal     @db.Decimal(12, 2)
  reason               String
  criteriaJson         Json?
  createdById          String?
  createdAt            DateTime    @default(now())
  appliedAt            DateTime?
  cancelledAt          DateTime?

  createdBy User?           @relation("GroupBonusCreator", fields: [createdById], references: [id], onDelete: SetNull)
  bonuses   DesignerBonus[]
}

model ProductionJob {
  id           String              @id @default(uuid())
  orderId      String
  orderItemId  String?
  status       ProductionJobStatus @default(ORDERED)
  queueType    String
  priority     Int                 @default(0)
  assignedOperatorId String?
  blockerReason String?
  failureReason String?
  customerSnapshotJson Json?
  productSnapshotJson Json?
  placementSnapshotJson Json?
  printAreaSnapshotJson Json?
  assetSnapshotJson Json?
  selectedOptionsJson Json?
  productionFileStatus String?
  productionFileAssetId String?
  productionFileObjectKey String?
  productionFileUrl String?
  productionFileJobId String?
  mockupPreviewUrl String?
  fulfillmentRoute OrderFulfillmentRoute @default(LOCAL_PRODUCTION)
  providerSyncRecordId String?
  providerType PodProviderType?
  providerOrderId String?
  providerStatus String?
  externalSourceType ExternalOrderSourceType?
  externalOrderId String?
  externalSku String?
  externalListingId String?
  externalChannelSnapshotJson Json?
  providerPayloadSnapshotJson Json?
  dueAt        DateTime?
  queuedAt     DateTime            @default(now())
  startedAt    DateTime?
  qcAt         DateTime?
  readyAt      DateTime?
  completedAt  DateTime?
  canceledAt   DateTime?
  qcStatus     String?
  qcNote       String?
  qcFailedReason String?
  qcCheckedById String?
  qcCheckedAt DateTime?
  producedQuantity Int?
  acceptedQuantity Int?
  rejectedQuantity Int?
  defectReason String?
  reprintOfProductionItemId String?
  deliveryProvider String?
  deliveryTrackingRef String?
  deliveryNote String?
  pickupNote String?
  statusHistoryJson Json?
  notes        String?
  createdAt    DateTime            @default(now())
  updatedAt    DateTime            @updatedAt

  order        Order      @relation(fields: [orderId], references: [id], onDelete: Cascade)
  orderItem    OrderItem? @relation(fields: [orderItemId], references: [id], onDelete: SetNull)
  providerSyncRecord PodProviderSyncRecord? @relation(fields: [providerSyncRecordId], references: [id], onDelete: SetNull)

  @@index([orderId, status])
  @@index([orderItemId])
  @@index([providerSyncRecordId])
  @@index([externalSourceType, externalOrderId])
  @@index([status, createdAt])
}

model CorporateRequest {
  id            String               @id @default(uuid())
  corporateUserId String
  title         String
  details       String?
  quantity      Int
  budget        Decimal?             @db.Decimal(10, 2)
  deadline      DateTime?
  status        CorporateRequestStatus @default(OPEN)
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt

  corporateUser User         @relation("CorporateClientRequests", fields: [corporateUserId], references: [id])
  bids          DesignerBid[]
  offers        CommercialOffer[]
}

model DesignerBid {
  id            String          @id @default(uuid())
  corporateRequestId String
  designerId    String
  proposal      String
  designFee     Decimal         @db.Decimal(10, 2)
  timelineDays  Int
  status        DesignerBidStatus @default(SUBMITTED)
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  corporateRequest CorporateRequest @relation(fields: [corporateRequestId], references: [id], onDelete: Cascade)
  designer         User             @relation("BidDesigner", fields: [designerId], references: [id])
  selectedInOffers CommercialOffer[]
}

model CommercialOffer {
  id            String              @id @default(uuid())
  corporateRequestId String
  selectedBidId String?
  offerNumber   String              @unique
  status        CommercialOfferStatus @default(DRAFT)
  subtotal      Decimal             @db.Decimal(10, 2)
  discount      Decimal             @db.Decimal(10, 2) @default(0)
  total         Decimal             @db.Decimal(10, 2)
  terms         String?
  sentAt        DateTime?
  acceptedAt    DateTime?
  rejectedAt    DateTime?
  createdAt     DateTime            @default(now())
  updatedAt     DateTime            @updatedAt

  corporateRequest CorporateRequest @relation(fields: [corporateRequestId], references: [id], onDelete: Cascade)
  selectedBid      DesignerBid?     @relation(fields: [selectedBidId], references: [id])
}

model WorkerJob {
  id          String          @id @default(uuid())
  type        String
  status      WorkerJobStatus @default(PENDING)
  idempotencyKey String?      @unique
  payloadJson Json
  attempts    Int             @default(0)
  maxAttempts Int             @default(3)
  errorMessage String?
  nextRunAt   DateTime        @default(now())
  lastErrorAt DateTime?
  claimedAt   DateTime?
  completedAt DateTime?
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

  @@index([type, status, nextRunAt])
  @@index([status, nextRunAt])
}

model AuditLog {
  id         String   @id @default(uuid())
  actorId    String?
  actorEmail String?
  actorRole  String?
  action     String
  entityType String
  entityId   String
  metadata   Json?
  ip         String?
  userAgent  String?
  createdAt  DateTime @default(now())

  actor User? @relation(fields: [actorId], references: [id])

  @@index([entityType, entityId])
  @@index([actorId])
  @@index([createdAt])
}

model PlatformSetting {
  key       String   @id
  value     Json
  updatedAt DateTime @updatedAt
}

model EmailTemplate {
  id        String   @id @default(uuid())
  key       String   @unique
  subject   String
  body      String
  variables Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model AiSetting {
  key       String   @id
  value     Json
  updatedAt DateTime @updatedAt
}

enum MediaCategory {
  UI_ASSET
  BRANDING_LOGO_WEB
  BRANDING_LOGO_DASHBOARD
  BRANDING_LOGO_LOGIN
  BRANDING_LOGO_FOOTER
  BRANDING_FAVICON
  BASE_PRODUCT_IMAGE
  MOCKUP_TEMPLATE
}

enum IntakeStatus {
  NEW
  IN_REVIEW
  CONTACTED
  APPROVED
  REJECTED
  ARCHIVED
}

enum SupportRequestStatus {
  OPEN
  IN_REVIEW
  WAITING_FOR_USER
  RESOLVED
  CLOSED
}

model MediaAsset {
  id           String        @id @default(uuid())
  key          String        @unique
  category     MediaCategory
  title        String
  description  String?
  bucket       String
  objectKey    String
  publicUrl    String?
  mimeType     String
  sizeBytes    Int
  width        Int?
  height       Int?
  uploaderId   String?
  isActive     Boolean       @default(true)
  sortOrder    Int           @default(0)
  metadata     Json?
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  @@index([category, isActive])
  @@index([category, sortOrder])
}

model DesignerApplication {
  id                 String       @id @default(uuid())
  firstName          String
  lastName           String
  email              String
  phoneCountryCode   String?
  phoneNumber        String?
  telegramUsername   String?
  passwordProvided   Boolean      @default(false)
  displayName        String?
  country            String?
  city               String?
  designCategories   Json?
  shortBio           String?
  portfolioFiles     Json?
  identityFiles      Json?
  selfieFiles        Json?
  confirmations      Json?
  status             IntakeStatus @default(NEW)
  reviewNotes        String?
  submittedAt        DateTime     @default(now())
  updatedAt          DateTime     @updatedAt

  @@index([status, submittedAt])
  @@index([email])
}

model ContactMessage {
  id          String       @id @default(uuid())
  firstName   String
  lastName    String?
  email       String
  phoneNumber String?
  subject     String
  message     String
  status      IntakeStatus @default(NEW)
  reviewNotes String?
  submittedAt DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  @@index([status, submittedAt])
  @@index([email])
}

model CustomOrderRequest {
  id                  String       @id @default(uuid())
  fullName            String
  companyEventName    String?
  email               String
  phoneCountryCode    String?
  phoneNumber         String?
  details             String?
  estimatedBudget     String?
  preferredDelivery   String?
  productNeed         String?
  quantity            String?
  deadline            DateTime?
  hasDesign           String?
  designTypes         String?
  uploadedFiles       Json?
  status              IntakeStatus @default(NEW)
  reviewNotes         String?
  submittedAt         DateTime     @default(now())
  updatedAt           DateTime     @updatedAt

  @@index([status, submittedAt])
  @@index([email])
}
